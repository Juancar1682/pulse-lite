commit 1: fixing the existing PUT /patient/:id that's still using the old combined schema
commit 2: update the delete to soft delete
commit 3: Update GET /vitals → GET /patients with JOIN to latest reading
